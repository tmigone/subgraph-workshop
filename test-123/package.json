{
  "name": "test-123",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-123",
    "create-local": "graph create --node http://localhost:8020/ test-123",
    "remove-local": "graph remove --node http://localhost:8020/ test-123",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-123",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
